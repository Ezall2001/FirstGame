#include "../../headers/dev.h"

int map_collision()
{
  ///CONTRA: return 0 if there is no collision, 1 if the player can't move any further
  return 0;
}

void proto1(char scn[])
{
  if (strcmp(scn, "proto1") == 0)
  {
    ///BILEL: make the player animation here
  }
}